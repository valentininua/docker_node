services:
    node:
        build:
            context: .
            dockerfile: ./Dockerfile
        tty: true
        ports:
            - "3000:3000"
        working_dir: /var/www
        volumes:
            - .:/var/www # Монтируем локальную папку в контейнер как /var/www
